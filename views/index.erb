<h3>Welcome to Pizza Shop!</h3>

<div class="row">
    <% @product.each do |p| %>
    <div class="col-md-4"> 
        <%= p.title %>
    </div>
    <% end %>
</div>

<button class="btn btn-primary" onclick="something()" > Click me! </button>